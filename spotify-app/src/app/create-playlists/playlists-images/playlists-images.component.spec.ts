/* import { ComponentFixture, TestBed } from '@angular/core/testing';

import { PlaylistsImagesComponent } from './playlists-images.component';

describe('PlaylistsImagesComponent', () => {
  let component: PlaylistsImagesComponent;
  let fixture: ComponentFixture<PlaylistsImagesComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ PlaylistsImagesComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(PlaylistsImagesComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */